[tool.poetry]
name = "nataraj-gen-ai-agents"
version = "0.1.2"
description = "A test GenAI Agents package"
authors = ["Nataraj"]
readme = "README.md"
packages = [{include = "gen_ai_agents"}]

[tool.poetry.dependencies]
python = "^3.9"
langchain = "*"
openai = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
